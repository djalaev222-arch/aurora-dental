PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Optional: leave empty to skip email sending (bookings are still saved to data/bookings.json)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CLINIC_NOTIFY_EMAIL=
